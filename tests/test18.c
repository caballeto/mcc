void printint(long x);

int main() {
	int x = 28, y = x +;

	for (int i = 0, j = 0; i < 10; i = i + 1) {
	  printint(i * j);
	  printint(k++)
	}

	int z = 100.;

	while (z < 100) {
	  z = z 1;
	  printint(z);
	}

	printint(k);
}