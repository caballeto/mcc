void printint(long x);

int fib(int x) {
  if (x == 0) {
    return x;
  } else if (x == 1) {
    return x;
  } else {
    return fib(x - 1) + fib(x - 2);
  }
}

int main() {
  int z = fib(10);
  printint(z);
}